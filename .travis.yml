language: node_js
node_js: '12'
cache: yarn

install:
  - yarn install

script:
  - yarn lint
  - yarn build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master
